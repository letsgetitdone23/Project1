Resolved improvements:
- Removed health visibility from frontend-facing surfaces.
- Location input is dropdown-driven from `GET /v1/cities` with 30-city behavior and full fallback list.
- Budget is numeric cost-for-two input and used in normalization/retrieval.
- `top_k` is optional; if omitted, backend returns all ranked matches.
- Model candidate shortlist remains fixed at 40 for LLM ranking context.

Additional runtime hardening:
- No-match recommendation requests return `200` with empty recommendations and guidance summary (no frontend-breaking 404).
- Streamlit deployment path includes DB-readiness error handling for safer operations.
- Next.js frontend no longer displays request id / timing trace text (`Request ... | ...ms`) to end users.